const authController = require("./auth.controller")
const userController = require("./user.controller")
const taskController = require("./task.controller")

module.exports = { authController, userController, taskController }
